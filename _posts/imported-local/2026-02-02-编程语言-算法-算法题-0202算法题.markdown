---
layout: "post"
title: "0202算法题"
subtitle: "编程语言 / 算法 / 算法题"
date: "2026-02-02 19:21:23"
author: "XGuider"
header-img: "img/post-bg.jpg"
catalog: true
tags:
    - 算法
    - 算法题
categories:
    - 编程语言
---

> 来源：`本机相关/01-编程语言/05-算法/算法题/0202算法题.md`

今日算法题（Day 29）
题目编号： LeetCode 123
题目名称： 买卖股票的最佳时机 III
难度： 🔴 困难
标签： 数组、动态规划
📝 题目描述
给定一个数组，它的第 i 个元素是一支给定的股票在第 i 天的价格。
设计一个算法来计算你所能获取的最大利润。你最多可以完成 两笔 交易。
注意：你不能同时参与多笔交易（你必须在再次购买前出售掉之前的股票）


输入： prices = [3,3,5,0,0,3,1,4]
输出： 6
解释： 在第 4 天（股票价格 = 0）的时候买入，在第 6 天（股票价格 = 3）的时候卖出，这笔交易所能获得利润 = 3-0 = 3。
随后，在第 7 天（股票价格 = 1）的时候买入，在第 8 天（股票价格 = 4）的时候卖出，这笔交易所能获得利润 = 4-1 = 3。
总利润为 3 + 3 = 6。



1、边界条件
1、只能完成0次交易；
2、只能完成1次交易；
3、只能完成2次交易；

class Solution {
    public int maxProfit(int[] prices) { // 修正1: 关键字拼写
        if (prices == null || prices.length == 0) { // 修正2: 检查顺序
            return 0;
        }

        // 修正3: 设置正确的初始状态
        int buy1 = -prices[0];   // 第一次买入，花费 prices[0]
        int sell1 = 0;           // 第一次卖出，初始为 0
        int buy2 = -prices[0];   // 第二次买入，初始时假设刚买入（虽然没卖出第一次）
        int sell2 = 0;           // 第二次卖出，初始为 0

        for (int i = 1; i < prices.length; i++) { // 从第 1 天开始遍历
            // 状态转移方程
            buy1 = Math.max(buy1, -prices[i]);          // 维持第一次持有，或在今天第一次买入
            sell1 = Math.max(sell1, buy1 + prices[i]);  // 维持第一次卖出，或在今天第一次卖出
            buy2 = Math.max(buy2, sell1 - prices[i]);   // 维持第二次持有，或用第一次的利润在今天第二次买入
            sell2 = Math.max(sell2, buy2 + prices[i]);  // 维持第二次卖出，或在今天第二次卖出
        }

        return sell2; // 返回第二次卖出后的最大利润
    }
}
