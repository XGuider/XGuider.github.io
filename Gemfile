source "https://rubygems.org"

# 使用GitHub Pages提供的gem包，确保与GitHub Pages环境一致
gem "github-pages", group: :jekyll_plugins

# 本地开发时如果需要，可以取消注释
# gem "jekyll", "~> 4.3.0"

# 插件（如果github-pages中已包含，则无需单独指定）
# gem "jekyll-paginate"
# gem "jekyll-feed", "~> 0.12"
# gem "jekyll-sitemap"

# Windows 和 JRuby 不支持，可以注释掉
# gem "wdm", "~> 0.1.0", :platforms => [:mingw, :x64_mingw, :mswin]

# 性能优化
gem "jekyll-gist"
